
enum AppRoutes {
   Home = "/",
   Products = "/products",
   Sales = "/sales",
   Cart = "/cart",
   About = "/about",
   ItemTemplate = "/item/:id",
   ItemBase = "/item/"
}

export default AppRoutes;