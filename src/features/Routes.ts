
enum AppRoutes {
   Home = "/",
   Products = "/products",
   Sales = "/sales",
   Cart = "/cart",
   About = "/about",
   ItemTemplate = "/item/:id",
   ItemBase = "/item/",
   Search = "/search",
}

export default AppRoutes;